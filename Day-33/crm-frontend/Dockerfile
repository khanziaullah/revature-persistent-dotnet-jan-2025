## ---- base: install deps + build shared types ----
FROM node:22-alpine AS base
WORKDIR /app
COPY package*.json ./
RUN npm install
COPY . .
RUN npm run build

## ---- prod: nginx serving only static assets ----
FROM nginx:alpine AS prod
COPY --from=base /app/dist /usr/share/nginx/html
EXPOSE 80
CMD ["nginx", "-g", "daemon off;"]
